Property Invariant Hypothesis

What mathematical properties must always hold for the following algorithm,
regardless of input values? List them as testable propositions.

Algorithm: [[METHOD_SIGNATURE_AND_BODY]]

For each property:
- Name the property (symmetry, bounds, monotonicity, idempotence, etc.)
- State it formally: score(A,B) == score(B,A)
- State it as a Kotest checkAll block using Arb generators